# Manager environment
# Edit these before first run. start.sh will prompt if API_ID/API_HASH are empty.

# Flask
PORT=5001
SECRET_KEY=change-me-please
# Optional: protect /api/ingest with a token (leave blank to disable)
INGEST_TOKEN=

# Telegram (used to manage live sessions)
API_ID=
API_HASH=
BOT_TOKEN=
LOG_CHANNEL_ID=
